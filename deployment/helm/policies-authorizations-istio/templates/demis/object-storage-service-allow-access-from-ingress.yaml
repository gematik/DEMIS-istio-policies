{{- if and (eq .Values.demis.enabled true) (eq .Values.demis.local.oss.enabled true) }}
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: object-storage-service-allow-access-from-ingress
  labels:
    component: demis
spec:
  selector:
    matchLabels:
      app: object-storage-service
  action: ALLOW
  rules:
  - from:
    - source:
        principals: 
         - cluster.local/ns/{{ .Release.Namespace }}/sa/igs-service*
         - cluster.local/ns/istio-system/sa/istio-ingressgateway
    to:
    - operation:
        ports: ["8080"]
{{- end }}
