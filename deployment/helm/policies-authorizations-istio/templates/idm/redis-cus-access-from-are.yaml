{{- if and (eq .Values.idm.enabled true) (eq .Values.tcp.principals.enabled true) }}
{{/* Allows the ARE NPS to access Redis in the IDM Namespace */}}
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: redis-cus-allow-access-from-are
  labels:
    component: idm
spec:
  selector:
    matchLabels:
      app: redis-cus
  action: ALLOW
  rules:
  - from:
    - source:
        principals:
          {{- range .Values.idm.areNamespaces }}
            - "cluster.local/ns/{{ . }}/sa/are-notification-processing-service*"
          {{- end }}
    to:
    - operation:
        # TCP Only
        ports: ["6379"]
{{- end }}
